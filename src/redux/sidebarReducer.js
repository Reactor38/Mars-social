let initialState = {
    friends: [
        { id: 1, name: 'Dmitry' },
        { id: 2, name: 'Kostya' },
        { id: 3, name: 'Julia' },
    ]
};

const sidebarReducer = (state = initialState, action) => {

    return state;
}

export default sidebarReducer;